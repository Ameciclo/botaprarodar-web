import { makeStyles } from '@material-ui/core';

export default makeStyles({
  cardsColumnsStyle: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginTop: '20px',
  },
});
