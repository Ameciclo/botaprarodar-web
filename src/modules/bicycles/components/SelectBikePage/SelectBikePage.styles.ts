import { makeStyles } from '@material-ui/core';

export default makeStyles(theme => ({
  cardPosition: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  titleStyle: {
    paddingBottom: '20px',
  },
}));
